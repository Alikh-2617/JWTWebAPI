using System.Security.Cryptography;

namespace JWTWebAPI.Services
{
    public class AuthService : IAuthService
    {
        // for registering new user with userName and pass (To get back hash pass in Array byte and salt in Array byte )
    }
}
